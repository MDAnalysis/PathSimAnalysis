name: pathsimanalysis-test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip

  # MDAnalysis
  - MDAnalysis
  - scipy>=1.5.0
  - matplotlib>=1.5.1
  - numpy>=1.22.3


  # Testing
  - MDAnalysisTests
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  # Pip-only installs
  #- pip:
  #  - codecov

